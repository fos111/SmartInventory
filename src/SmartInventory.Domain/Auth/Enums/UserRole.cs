namespace SmartInventory.Domain.Auth.Enums;

public enum UserRole
{
    Technicien = 0,
    Supervisor = 1,
    Gestionnaire = 2
}

public enum AccountStatus
{
    Pending = 0,
    Active = 1,
    Rejected = 2
}